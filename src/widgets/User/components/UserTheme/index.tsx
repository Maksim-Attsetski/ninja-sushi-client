import React, { FC, memo } from 'react';

import s from './UserTheme.module.scss';

const UserTheme: FC = () => {
  return (
    <div>
      <div>UserTheme</div>
    </div>
  );
};
export default memo(UserTheme);
